/**
 * Typography tokens.
 * Font families are configured via next/font in layout.tsx and exposed as CSS variables.
 * These constants are for programmatic reference only.
 */

export const fontFamily = {
  sans: 'var(--font-inter), system-ui, sans-serif',
  mono: 'var(--font-jetbrains-mono), monospace',
} as const;
